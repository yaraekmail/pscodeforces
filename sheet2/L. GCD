https://codeforces.com/group/MWSDmqGsZm/contest/219432/problem/L

L. GCD
#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int x, y;
    cin >> x >> y;
    cout << gcd(x, y);
    return 0;
}
