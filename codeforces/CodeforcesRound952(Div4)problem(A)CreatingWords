#include<iostream>
using namespace std;
 
int main() {
 
int x;
cin>>x;
for(int i=0;i<x;i++){
string a,b;
char a1,b1;
    cin >> a>> b;
   
    a1 = b[0];
    b1 = a[0];
    a.erase(0, 1);
    b.erase(0, 1);
    cout << a1 + a + " " + b1 + b<<endl;}
  return 0;
}